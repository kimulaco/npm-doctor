{
  "name": "npm-doctor",
  "version": "0.0.1",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "start": "lerna run start --parallel --scope api",
    "build": "lerna run build",
    "dev": "lerna run dev --parallel",
    "test": "lerna run test"
  },
  "dependencies": {
    "lerna": "^3.20.2"
  }
}
